@model EvaluationsShowDomainsPerEvaluationViewModel

@{
    var rnd = new Random();
}

<div class="evaluation-title-box">
    @* TODO: once login system: redirect to Evaluations/Evaluations method*@
    <a asp-controller="Home" asp-action="Index" class="btn btn-info">
        ←
    </a>
    <h1>Domeinen</h1>
    <p class="evaluation-subtitle"><sub class="evaluation-subtitle">(@Model.Evaluation.Title)</sub></p>
</div>

<div style="display:flex">
    @foreach (var domain in Model.Evaluation.CompetenceDomains)
    {
        <div class="card module-box">
            @* TODO: direct to tests *@
            <a asp-controller="TODO" asp-action="TODO" asp-route-id="TODO" class="link-restyle">
                <div class="title-box gradient-@rnd.Next(1, 16) text-center">
                    <h3>@domain.Name</h3>
                </div>
                @* <div class="domain-box">
                    <h7 class="text-muted">Beschikbaar tot: @domain. .Value.ToString("d")</h7>
                </div> *@
            </a>
        </div>
    }
</div>